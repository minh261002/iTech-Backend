---
deployment:
  tasks:
    - export DEPLOYPATH=/home/minhdevi/itech.minhdev.top/

    - /bin/cp -r * $DEPLOYPATH
    - /bin/cp -r .[^.]* $DEPLOYPATH
    - /bin/cp -r .cpanel.yml $DEPLOYPATH
    - /bin/cp -r .htaccess $DEPLOYPATH
